# Ray Tracing on GPU

This is a simple ray tracing implementation using OpenGL compute shaders on the GPU.

## build and run

Hey, to get this running:

1. Open `ray-tracing-on-gpu.sln` in Visual Studio.
2. Make sure the configuration is set to x64.
3. Hit build and run.